from django.urls import path
from . import views

app_name = 'users'

urlpatterns = [
    #path('', views.LivrosListView.as_view(), name='list'),
    #path('<slug:slug>/', views.LivrosDetailView.as_view(), name='detail'}
]